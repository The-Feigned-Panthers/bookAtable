export class Booking {
    userId: string;
    restaurantId: string;
    date: Date;
    time: string;
    numberOfClients: number;

    constructor(userId, restaurantId, date, time, numberOfClients) {}
}
